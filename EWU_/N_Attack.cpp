#include <bits/stdc++.h>
using namespace std;

int main() {
    long long int A, B;
    cin >> A >> B;
    long long int temp = (ceil(((long double) A) / B));
    cout << temp << endl;
    return 0;
}